class Solution:
    def evenlyDivides(self, n):
        
        s=str(n)
        c = 0
        for a in s:
            d = int(a)
            if d != 0 and n % d == 0:
                c+=1
        return c
